VITE_SITE_URL=http://localhost:3000
VITE_GA4_ID=
VITE_GOOGLE_SITE_VERIFICATION=
VITE_ENABLE_SANDBOX=false

# TuneClue product flags.
# Keep false until the production TikTok extractor gate passes.
VITE_ENABLE_TIKTOK=false

# Server-only recognition secret. Never expose as VITE_*.
AUDD_API_TOKEN=

# Google OAuth web application credentials. Server-only.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Internal TikTok extractor PoC. Server-only.
# The public TikTok feature remains disabled while this is tested.
TIKTOK_POC_TOKEN=

# Payment phase (not production-ready until identity + ledger + legal gates pass).
PANCAKE_API_KEY=
PANCAKE_WEBHOOK_SECRET=
PANCAKE_PRODUCT_ID=
